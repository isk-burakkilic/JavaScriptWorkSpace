//! MANTIKSAL OPERATÖRLER


    //? && : ve demek hepsi true olmalı.

    //? || : veya demek bir tane true varsa sonuç true olur.

    //? ! : değil demek bir değerin tersini alır.

// yaşınız 18'den büyük ve 3000 tl para olması iki tarafta true olmalı.

let yas = 20;
let para= 3500;
let saglikliMi= false;
let haksayisi=3;
 console.log(yas>18 && para > 3000 && saglikliMi==true);


//* console.log(5>3 || 10<3); //veya 

//* console.log(!(5>3)); // değil örneği ünlem tersini alır.


//! Karşılaştırma Operatörleri

/*
    == : eşit mi

    != : eşit değil mi?  eşit değilse

    > : büyük mü
    
    < : küçük mü

    >= büyük eşit mi

    <= küçük eşit mi

*/